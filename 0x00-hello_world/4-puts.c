#include <stdio.h>
/**
 * main - Entry point for all C programs
 * Return: Returns 0 when the command is succesful
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
